# PatikaSQL-Challenges
Patika's SQL Challenges for study


First question answer : SELECT * FROM Country
                        WHERE country LIKE 'A%a'

Second question answer : SELECT * FROM Country
                         WHERE country LIKE '______%n'

Third question answer : SELECT title FROM film
                        WHERE title ILIKE '%t%t%t%t%'

Fourth question answer : SELECT * FROM film
                         WHERE title LIKE 'C%' AND length > 90 AND rental_rate = 2.99
