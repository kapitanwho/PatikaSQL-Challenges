# PatikaSQL-Challenges
Patika's SQL Challenges for study

First question answer : SELECT title,length FROM film
                        WHERE title ILIKE '%n'
                        ORDER BY length DESC
                        LIMIT 5

Second question answer : SELECT title,length FROM film
                         WHERE title ILIKE '%n'
                         ORDER BY length ASC
                         OFFSET 5
                         LIMIT 5

Third question answer : SELECT last_name, store_id FROM customer
                        WHERE store_id = 1
                        ORDER BY last_name DESC
                        LIMIT 4
